export interface User {
    userid?: string,
    username?: string,
    type?: string,
}


/*Type的类型*/
export const user_type: string[] = [
    'all',
    'admin',
    'maker',
    'checker',
    'maker_checker',
]

